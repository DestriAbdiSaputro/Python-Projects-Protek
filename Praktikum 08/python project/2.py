myData =[]
def dataStat(myData):
    data =[]
    sum = 0
    i = 0
    maksimal = 0
    minimum = 100
    nilai = int(input('Masukkan bilangan bulat: '))
    data = data + [nilai]
    for angka in data:
        sum += data
        i +=1
    rata2 = sum/i
    for data in mydata:
        if data > maksimal:
            maksimal = data
    maks = maksimal
    for data in myData:
        if data < minimum:
            minimum = data
    min = minimum


dataStat(myData)
